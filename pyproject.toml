[project]
name = "my-python-service"
version = "0.1.0"
description = "Next.js code review MCP service with GitLab MR integration"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.128.8",
    "httpx>=0.28.1",
    "mcp>=1.2.0",
    "uvicorn[standard]>=0.39.0",
]
